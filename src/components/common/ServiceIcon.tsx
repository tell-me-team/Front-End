import { styled } from "styled-components";

const ServiceIcon = () => {
  return <SServiceIcon />;
};

const SServiceIcon = styled.div`
  width: 52px;
  height: 52px;
  background-image: url(/tellme_icon.svg);
  background-repeat: no-repeat;
  background-size: cover;
  border-radius: 50%;
`;

export default ServiceIcon;
